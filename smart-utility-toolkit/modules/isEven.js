// modules/isEven.js
// Custom module to check whether a number is even or odd

function isEven(num) {
  return num % 2 === 0;
}

// Exporting the function so it can be reused in other files
module.exports = isEven;
